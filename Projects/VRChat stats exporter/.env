# ╔══════════════════════════════════════════════════════════════╗
# ║  VRChat Monitoring - Environment Configuration              ║
# ╚══════════════════════════════════════════════════════════════╝

# ── Authentication ────────────────────────────────────────────────────────────
#
# OPTION 1: Auth cookie (RECOMMENDED - avoids session limits & 2FA issues)
#   1. Log into https://vrchat.com in your browser
#   2. Open DevTools (F12) > Application > Cookies > https://vrchat.com
#   3. Copy the value of the "auth" cookie
#
VRCHAT_AUTH_COOKIE=

# OPTION 2: Username + Password
#   Warning: each login creates a new session and counts against rate limits.
#   Will NOT work if your account has 2FA enabled — use the cookie instead.
#
# VRCHAT_USERNAME=your_username
# VRCHAT_PASSWORD=your_password

# ── Required: User-Agent string (VRChat ToS requirement) ─────────────────────
#   Format: AppName/Version ContactInfo
#   Use your Discord username, email, or GitHub handle as contact info.
#
VRCHAT_USER_AGENT=VRChatMonitor/2.0 mydiscordusername

# ── Exporter settings ─────────────────────────────────────────────────────────

# Port Prometheus scrapes metrics from
EXPORTER_PORT=9101

# Seconds between full scrape cycles (minimum recommended: 60)
SCRAPE_INTERVAL=120

# Seconds between individual API requests within a cycle (recommended: 1.5–2.0)
REQUEST_DELAY=1.5

# ── Collection tuning ─────────────────────────────────────────────────────────

# Max new world IDs to resolve per cycle (cached permanently after first lookup)
MAX_WORLD_LOOKUPS=40

# Max new avatar IDs to resolve per cycle
MAX_AVATAR_LOOKUPS=20

# Max new group IDs to resolve per cycle
MAX_GROUP_LOOKUPS=10

# How many scrape cycles between offline-friends fetches (they change slowly)
OFFLINE_SCRAPE_CYCLES=5